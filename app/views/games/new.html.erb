<!DOCTYPE html>
<html>
<head>
  <title>Longest Word Game</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
  <%= stylesheet_link_tag 'application', media: 'all' %> <!-- This links to your application.css -->
</head>
<body>
  <div class="container mt-5">
    <h1 class="mb-4">New game</h1>
    <div class="mb-3">
      <% @letters.each do |letter| %>
        <div class="letter-box"><%= letter %></div>
      <% end %>
    </div>

    <p>What is the longest word you can find?</p>

    <form action="/score" method="post" data-turbo="false" class="d-flex">
      <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
      <%= hidden_field_tag :letters, @letters.join('') %>

      <input type="text" name="word" class="form-control me-2" placeholder="Enter your word">
      <button type="submit" class="btn btn-primary">Play</button>
    </form>
  </div>
</body>
</html>
